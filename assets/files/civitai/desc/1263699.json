{"h":"<p>在 ComfyUI 中训练 FLUX。简单的工作流程，没有添加正则化图像。<br><br><strong>v2.0 发行说明：</strong><br>修复 Flux-Trainer VisualizeLoss 无法记录全局损失的错误。 v1.0工作流程仅显示当前环路损耗，无法自动刷新丢失数据，因此它始终显示第一个环路损耗。</p><p>v2.0工作流程修复了这个问题。但你应该修改 Flux-Trainer 代码。</p><pre><code>可视化损失类：\n@类方法\n...\n# 改变输出类型\nRETURN_TYPES = (\"NETWORKTRAINER\", \"IMAGE\", \"FLOAT\",)\nRETURN_NAMES = (\"network_trainer\", \"plot\", \"loss_list\",)\n功能=“画”\n类别 =“FluxTrainer”\ndef绘制（自我，network_trainer，window_size，plot_style，normalize_y，宽度，高度，log_scale）：\n...\n# 添加代码\n训练循环=网络训练器[“训练循环”]\n网络训练器=网络训练器[“网络训练器”]\n...\n# 它会让loss list变成所有相同常量的列表，我不知道如何修改它，暂时关闭它。\n#\n# 如果窗口大小 > 0:\n# loss_values = moving_average(loss_values, window_size)\n...\n教练={\n“网络训练器”：网络训练器，\n“训练循环”：训练循环，\n}\n# 改变输出\n返回（训练器，图像张量，损失值，）</code></pre>","lang":"en","v":2,"o":{"en":"<p>Training FLUX in ComfyUI. A brief workflow, didn't add regularization image.<br><br><strong>v2.0 release note:</strong><br>Fix Flux-Trainer VisualizeLoss can't record global loss bug. v1.0 workflow just show current loop loss and can't auto flash loss data, so it always show first loop loss.</p><p>v2.0 workflow fix this problem. But you should modify Flux-Trainer code.</p><pre><code>class VisualizeLoss:\n@classmethod\n...\n# change output types\nRETURN_TYPES = (\"NETWORKTRAINER\", \"IMAGE\", \"FLOAT\",)\nRETURN_NAMES = (\"network_trainer\", \"plot\", \"loss_list\",)\nFUNCTION = \"draw\"\nCATEGORY = \"FluxTrainer\"\ndef draw(self, network_trainer, window_size, plot_style, normalize_y, width, height, log_scale):\n...\n# add code\ntraining_loop = network_trainer[\"training_loop\"]\nnetwork_trainer = network_trainer[\"network_trainer\"]\n...\n# It will make loss list become a list of all the same constants, I don't know how to modify it, temporarily shut down it.\n#\n# if window_size &gt; 0:\n# loss_values = moving_average(loss_values, window_size)\n...\ntrainer = {\n\"network_trainer\": network_trainer,\n\"training_loop\": training_loop,\n}\n# change output\nreturn (trainer, image_tensor, loss_values,)</code></pre>"}}